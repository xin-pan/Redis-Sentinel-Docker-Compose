version: '3.7'

services:
 
  redis-master:
    image: redis:6-alpine
    volumes:
      - "./.master-data:/data"
    ports:
      - "6379:6379"

#  redis-slave:
#    image: redis:6-alpine
#    command: redis-server --slaveof redis-master 6379
#    links:
#      - redis-master
#    volumes:
#      - "./.slave-data:/data"
     
  # Instance 1
  redis-sentinel:
    build:
      context: ./redis-sentinel
    links:
      - redis-master
    ports:
      - "26379:26379"
    environment:
      - MASTER_HOST
    env_file:
      - .env
  # Instance 2
  redis-sentinel2:
    build:
      context: ./redis-sentinel
    links:
      - redis-master
    ports:
      - "26380:26379"
    environment:
      - MASTER_HOST
    env_file:
      - .env

  # Instance 3
  redis-sentinel3:
    build:
      context: ./redis-sentinel
    links:
      - redis-master
    ports:
      - "26381:26379"
    environment:
      - MASTER_HOST
    env_file:
      - .env
